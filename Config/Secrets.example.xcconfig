// Secrets.example.xcconfig — committed template.
//
// Setup (una sola vez):
//   cp Config/Secrets.example.xcconfig Config/Secrets.xcconfig
//   # pega los valores reales (pídelos al owner / Railway → maximus-api → Variables)
//
// Secrets.xcconfig está en .gitignore y nunca se commitea.

MAXIMUS_API_KEY =
MAXIMUS_API_SECRET =
